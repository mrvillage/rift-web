export interface SideBarItem {
  name: string;
  icon: string;
  path: string;
}

export interface Guild {
  id: number;
  name: string;
}

export interface GuildItem {
  text: string;
  value: number;
}
